Tested only on Linux

java jdk 11 needed for building jars

.env structure{
access_token=<access_token> #creatable over github developer settings
repo=German-Immersive-Railroading-Community/GIRSignals
}

all used pip packages are in libs_pip
